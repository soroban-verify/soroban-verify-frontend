# Base URL of the soroban-verify public REST API.
# Leave unset during early development to use the built-in mock data.
VITE_API_URL=

# Default network shown in the explorer: "mainnet" | "testnet"
VITE_DEFAULT_NETWORK=testnet
